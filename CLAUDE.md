# Claude Contract for Godot Game Test Lab

Read `AGENTS.md` and `evavo.reliability.json` before acting.

Work directly on `main`, never create a branch or repository, use `--ff-only`, never force-push, and push each tested coherent commit immediately. Preserve unrelated work.

Use this repository for native Godot inspection, Godot Mono/.NET validation, bounded boot, export and movie evidence. Use Godot Web Runtime for interactive browser playtesting. Never claim visual or interactive proof from a headless import, and never edit a target game without Development Studio authority.
