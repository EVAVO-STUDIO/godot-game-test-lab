# Claude Contract for Godot Game Test Lab

Read `AGENTS.md` and `evavo.reliability.json` before acting.

Work directly on `main`, preserve unrelated work and publish only through Development Studio `mainline-publish` with an explicit portfolio operation and exact changed paths. Do not create branches, pull requests, repositories or raw pushes. Require the Godot lab repository lease and stop when publication, provider or Sentinel evidence is unavailable.

Use this repository for native Godot inspection, Godot Mono/.NET validation, bounded Boot, export and movie evidence. Use Godot Web Runtime for compatible browser playtesting. Never claim Windows, Godot, visual or interactive proof from the Python source workflow or a headless import.

A real target run requires a freshly probed Windows x64 runner, the exact target main SHA, the target reliability profile, absolute target and external evidence paths, `godot-lab doctor`, the correct standard or Mono Godot executable and `.NET` when C# is detected.

Never edit, commit, push, deploy or mutate a target game without a separate Development Studio execution grant and the target repository’s own exclusive mainline lease.
